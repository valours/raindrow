import styled from 'styled-components';

const styles = {};

styles.Section = styled.div``;

styles.Title = styled.h1`
  padding: 10px;
`;

styles.Components = styled.div`
  display: flex
`

export default styles